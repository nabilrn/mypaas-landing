---
import { siCaddy, siCloudflare, siDocker, siGithub, siLinux, siPodman, siPostgresql, siUbuntu } from "simple-icons";
const items = [
  ["GitHub", siGithub],
  ["Cloudflare", siCloudflare],
  ["Docker", siDocker],
  ["Podman", siPodman],
  ["Caddy", siCaddy],
  ["PostgreSQL", siPostgresql],
  ["Linux", siLinux],
  ["Ubuntu", siUbuntu],
] as const;
---
<div class="integration-strip" aria-label="MyPaaS integrations and runtime ecosystem">
  <p>Works with GitHub, Cloudflare, and MCP-compatible agent workflows.</p>
  <div class="logo-row">
    {items.map(([name, icon]) => (
      <div class="logo-tile" style={`--brand:#${icon.hex}`} title={name} aria-label={name}>
        <svg viewBox="0 0 24 24" aria-hidden="true"><path d={icon.path} /></svg>
        <span>{name}</span>
      </div>
    ))}
    <div class="logo-tile mcp" title="Model Context Protocol" aria-label="Model Context Protocol">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M12 2v20M2 12h20M4.9 4.9l14.2 14.2M19.1 4.9 4.9 19.1" />
        <path d="M8.8 2.9 12 12l3.2 9.1M21.1 8.8 12 12 2.9 15.2" />
      </svg>
      <span>MCP</span>
    </div>
  </div>
</div>

<style>
.integration-strip{
  position:relative;
  z-index:12;
  display:grid;
  grid-template-columns:minmax(15rem,.72fr) minmax(0,2.65fr);
  gap:1rem;
  width:min(calc(100vw - 56px),86.5rem);
  margin:-36px 0 0 50%;
  padding:.82rem 1rem;
  transform:translateX(-50%);
  align-items:center;
  border:1px solid #d6d6d6;
  border-radius:16px;
  background:#fff;
  box-shadow:0 12px 28px rgb(0 0 0 / .1);
}
.integration-strip>p{max-width:16.5rem;margin:0;color:#262626;font-size:.86rem;line-height:1.42}
.logo-row{display:grid;grid-template-columns:repeat(9,minmax(4.35rem,1fr));gap:.46rem}
.logo-tile{
  display:grid;
  min-width:0;
  min-height:4.45rem;
  align-content:center;
  justify-items:center;
  gap:.34rem;
  padding:.52rem .3rem;
  border:1px solid #dedede;
  border-radius:10px;
  background:#fff;
  box-shadow:inset 0 1px 0 rgb(255 255 255 / .8);
}
.logo-tile svg{width:1.7rem;height:1.7rem;fill:var(--brand)}
.logo-tile span{max-width:100%;overflow:hidden;color:#111;font-size:.68rem;font-weight:650;text-overflow:ellipsis;white-space:nowrap}
.logo-tile.mcp svg{fill:none;stroke:#181818;stroke-width:1.45;stroke-linecap:square;stroke-linejoin:miter}

@media(max-width:1100px){
  .integration-strip{grid-template-columns:1fr;width:min(calc(100vw - 36px),62rem);margin-top:-30px;padding:.8rem}
  .integration-strip>p{max-width:38rem}
  .logo-row{grid-template-columns:repeat(5,1fr)}
}
@media(max-width:650px){
  .integration-strip{width:calc(100vw - 24px);margin-top:-22px;padding:.72rem;border-radius:14px}
  .logo-row{display:flex;overflow-x:auto;padding-bottom:.2rem;scrollbar-width:thin}
  .logo-tile{flex:0 0 5.1rem;min-height:4.3rem}
  .integration-strip>p{font-size:.82rem}
}
</style>
